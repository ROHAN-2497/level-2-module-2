export const add1 = (param1: number, param2: number): number => {
  return param1 + param2;
};

export const substract = (param1: number, param2: number): number => {
  return param1 - param2;
};

export const muliply = (param1: number, param2: number): number => {
  return param1 * param2;
};
